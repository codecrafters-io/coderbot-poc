on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      matrix:
        # course: ["redis", "docker", "git", "sqlite", "grep", "bittorrent"]
        # language: ["go", "python"]
        course: ["redis", "docker", "git", "sqlite", "grep", "bittorrent"]
        language: ["python"]

    steps:
      - uses: actions/checkout@v3

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.1" # diskcached doesn't support Ruby 3.2 yet
          bundler-cache: true # runs 'bundle install' and caches installed gems automatically

      - run: mkdir -p cache/prompts

      - name: Cache prompts
        uses: actions/cache@v3
        with:
          path: cache/prompts
          key: ${{ runner.os }}-prompts-${{ matrix.course }}-${{ matrix.language}}-${{ hashFiles('.github/workflows/test.yml', 'prompts/**') }}
          restore-keys: |
            ${{ runner.os }}-prompts-${{ matrix.course }}-${{ matrix.language}}-
            ${{ runner.os }}-prompts

      - run: ls cache/prompts

      - run: make generate_fixtures

      - run: mkdir -p test_repositories
      - run: cp -R fixtures/courses/${{ matrix.course }}/compiled_starters/${{ matrix.language }} test_repositories/${{ matrix.course }}
      - run: git -C test_repositories/${{ matrix.course }} init

      - name: Stage 1
        run: bundle exec ruby main.rb ${{ matrix.course}} 1 test_repositories/${{ matrix.course }}
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Commit Stage 1
        run: git -C test_repositories/${{ matrix.course }} commit -am "Stage 1"

      - name: Stage 2
        run: bundle exec ruby main.rb ${{ matrix.course}} 2 test_repositories/${{ matrix.course }}
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
